{% extends 'base.html.twig' %}

{% block body %}
    <div class="position-absolute top-0 start-50 translate-middle-x"><h1>Producten</h1></div>
    <div class="container-fluid">
        <div class="row mt-4 mb-5">
                <div class="col-md-2 text-center" >
                    <div class="card border-0">
                        <h3 class="text-center">
                            {{ product.name }}
                        </h3>
                        {#                        <img src="/img/{{ product.img }}" class="img img-fluid rounded" alt="Responsive image">#}
                        <div class="card-body">
                            <h4 class="text-center">

                            </h4>
                            <a class="btn btn-primary rowcolor text-center" href="#">kopen</a>
                        </div>
                    </div>
                </div>
        </div>
    </div>
{% endblock %}

